<{{ heading.tag ? heading.tag : heading.lvl ? "h#{heading.lvl}" : "h1" }} class="{% if not heading.hidden %}heading heading_lvl_{{ heading.lvl ?? "1" }} {{ headding.classes }}{% else %}visually-hidden{% endif %}">
	{{ heading.text ?? page.title }}
</{{ heading.tag ? heading.tag : heading.lvl ? "h#{heading.lvl}" : "h1" }}>
