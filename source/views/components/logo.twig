<a class="logo link {{ classes }}" href="{{ site.dir }}" {% if page.path == "" %}aria-current="page"{% endif %}>
	<svg class="logo__image" width="{{ w ?? 140 }}" height="{{ h ?? 56 }}"  aria-label="Логотип школы английского «Say Yes!»">
		<use href="{{ site.dir }}assets/images/logo.svg#{{ variant ?? "school" }}_{{ fill ?? "violet" }}" aria-hidden="true"/>
	</svg>
</a>
